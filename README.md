
# CairnPath + TriTRPC bundle v0.4

Includes:
- TriTRPC frame envelope schema
- CairnPath schemas: Context / Step / Line / Result / Materialize
- Policy schema: CairnLimits
- fixtures + validator

## Validate
```bash
python3 -m pip install jsonschema && python3 tools/validate.py
```
