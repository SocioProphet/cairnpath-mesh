# Profit MPCC lineage tranche intake

## Purpose

This note records the current relationship between `mdheller/profit-mpcc` and `SocioProphet/cairnpath-mesh` for the lineage/replay lane.

## Current stance

`profit-mpcc` is the upstream semantic/archive drafting root.

`cairnpath-mesh` remains the narrower lineage/replay schema bundle.

The relevant near-term export surfaces from `profit-mpcc` are:
- branch identity,
- overlap witnesses,
- conflict / contradiction objects,
- rewrite-log semantics,
- quote and redaction contracts,
- replay-oriented examples under the cairnpath tranche.

## Governance rule

Treat the `profit-mpcc` lineage tranche as upstream source material and import only stabilized lineage/replay contracts and fixtures.

Do not absorb archive-native or still-moving semantic drafting surfaces wholesale.
