# Profit MPCC lineage/replay imports

This directory stages narrow lineage/replay contracts imported from `mdheller/profit-mpcc`.

Current imported drafts:
- `overlap-witness.schema.v0.json`
- `conflict-object.schema.v0.json`
- `rewrite-log.schema.v0.json`

These are intake copies for review and alignment inside the replay lane. They are not yet the canonical upstream source of truth.
